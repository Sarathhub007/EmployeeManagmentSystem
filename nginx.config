server {
    listen 80;

    location /ems/ {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    # Optional: serve 404 for anything else
    location / {
        return 404;
    }
}
